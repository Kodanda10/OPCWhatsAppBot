import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kodanda10.opcwhatsappbot',
  appName: 'OPCWhatsAppBot',
  webDir: 'dist'
};

export default config;
